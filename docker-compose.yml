services:
  vue:
    image: node:20.19-alpine
    container_name: vue-dev
    working_dir: /app

    ports:
      - "5174:5173"

    volumes:
      - .:/app
      - /app/node_modules

    command: sh -c "npm install && npm run dev -- --host"

    stdin_open: true
    tty: true
